# How to run the simulation to collect the data

1. Install Node.js
```bash
brew install node
```

2. Install dependencies:
```bash
npm install
```
3. Run the app:
```bash
npm run dev
```


After downloading the data, save it in _Poli/data/_ folder and run file _jsonTonpy.py_ to convert the data from json to numpy format.